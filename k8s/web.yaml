apiVersion: apps/v1
kind: Deployment
metadata:
  name: licitaciones-web
  namespace: licitaciones
spec:
  replicas: 1
  selector:
    matchLabels:
      app: licitaciones-web
  template:
    metadata:
      labels:
        app: licitaciones-web
    spec:
      containers:
        - name: web
          image: licitaciones-web:v1.0.0-rc
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: licitaciones-config
            - secretRef:
                name: licitaciones-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: licitaciones-web
  namespace: licitaciones
spec:
  type: NodePort
  selector:
    app: licitaciones-web
  ports:
    - port: 8080
      targetPort: 8080
